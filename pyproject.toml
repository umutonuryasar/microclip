[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "microclip"
version = "0.1.0"
description = "CLIP-style VLM from scratch on a single-GPU budget"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
